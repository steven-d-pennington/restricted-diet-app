# Example environment variables for Restricted Diet App
# Copy this file to .env and fill in values for your environment.
# IMPORTANT: Do not commit your real .env file.

# Expo
EXPO_DEV_CLIENT=1

# Supabase (web/native use EXPO_PUBLIC_* in Expo)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=replace-with-local-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key

# Expo public (available to client)
EXPO_PUBLIC_SUPABASE_URL=http://localhost:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=replace-with-local-anon-key

# Optional: Device-only URL override so iOS/Android can reach Supabase (use your LAN IP)
# Example: http://192.168.1.50:54321
EXPO_PUBLIC_SUPABASE_DEVICE_URL=http://YOUR_LAN_IP:54321

# Database connection string (for scripts/migrations)
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:54322/postgres
